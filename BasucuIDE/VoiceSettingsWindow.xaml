<Window x:Class="mdaiAgent.VoiceSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{local:Loc Key=SesliKomutAyarlari}"
        Width="650" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False" xmlns:local="clr-namespace:mdaiAgent">

    <Window.Resources>
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1c2128"/>
            <Setter Property="Foreground" Value="#cdd9e5"/>
            <Setter Property="BorderBrush" Value="#373e47"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="#58a6ff"/>
            <Setter Property="SelectionBrush" Value="#264f78"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#58a6ff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#1c2128"/>
            <Setter Property="Foreground" Value="#cdd9e5"/>
            <Setter Property="BorderBrush" Value="#373e47"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="#58a6ff"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#58a6ff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SaveButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2ea043"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1a7f37"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="#21262d"/>
            <Setter Property="Foreground" Value="#cdd9e5"/>
            <Setter Property="BorderBrush" Value="#373e47"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#30363d"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Dış gölge efekti -->
    <Border Background="#0d1117"
            BorderBrush="#373e47"
            BorderThickness="1"
            CornerRadius="12"
            Padding="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="6" Opacity="0.7"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık Çubuğu (Sürüklenebilir) -->
            <Border Grid.Row="0" Background="#161b22" CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                    Cursor="SizeAll">
                <Grid Margin="20,14">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🎤" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="txtWindowTitle" Text="{local:Loc Key=SesliKomutAyarlari}"
                                       FontSize="14" FontWeight="SemiBold"
                                       Foreground="#cdd9e5"/>
                            <TextBlock x:Name="txtSubtitle"
                                       Text="{local:Loc Key=SesKaydiniMetneCevirmekIcinBirAPIGirin}"
                                       FontSize="11" Foreground="#8b949e"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Content="✕" Width="28" Height="28"
                            Style="{StaticResource CancelButton}"
                            Click="BtnClose_Click"
                            FontSize="12" Padding="0"/>
                </Grid>
            </Border>

            <!-- İçerik -->
            <StackPanel Grid.Row="1" Margin="24,20">

                <!-- Hızlı Öneri Rozeti -->
                <Border Background="#161b22" BorderBrush="#30363d" BorderThickness="1"
                        CornerRadius="8" Padding="12,8" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💡" FontSize="13" Margin="0,0,8,0"/>
                        <TextBlock x:Name="txtHintText" Foreground="#8b949e" FontSize="12" TextWrapping="Wrap">
                            <Run x:Name="runHintPrefix" Text="{local:Loc Key=Ipucu}"/>
                            <Run x:Name="runHintBody" Text="OpenAI-compatible "/>
                            <Run x:Name="runHintApi" Foreground="#58a6ff" Text=" Whisper API "/>
                            <Run x:Name="runHintSuffix" Text="works with many cloud or local services."/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Base URL -->
                <TextBlock x:Name="txtBaseUrlLabel" Text="Base URL" Foreground="#8b949e" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtSttBaseUrl"
                         Style="{StaticResource DarkTextBox}"
                         Margin="0,0,0,12"/>

                <!-- API Key -->
                <TextBlock x:Name="txtApiKeyLabel" Text="{local:Loc Key=APIAnahtari}" Foreground="#8b949e" FontSize="12" Margin="0,0,0,5"/>
                <PasswordBox x:Name="pbSttApiKey"
                             Style="{StaticResource DarkPasswordBox}"
                             Margin="0,0,0,12"/>

                <!-- Model -->
                <TextBlock x:Name="txtModelLabel" Text="{local:Loc Key=ModelAdi}" Foreground="#8b949e" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtSttModel"
                         Style="{StaticResource DarkTextBox}"
                         Margin="0,0,0,0"/>
            </StackPanel>

            <!-- Alt Butonlar -->
            <Border Grid.Row="2" Background="#161b22" CornerRadius="0,0,12,12"
                    BorderBrush="#30363d" BorderThickness="0,1,0,0" Padding="24,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnCancel" Content="{local:Loc Key=Cancel}" Style="{StaticResource CancelButton}"
                            Width="90" Click="BtnClose_Click" Margin="0,0,10,0"/>
                        <Button x:Name="btnSave" Content="{local:Loc Key=Kaydet2}" Style="{StaticResource SaveButton}"
                            Width="110" Click="BtnSave_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
