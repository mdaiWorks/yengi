<Window x:Class="mdaiAgent.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Preview" 
        Height="800" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DarkPanelBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource DarkPanelBackground}" Padding="8" BorderBrush="{StaticResource DarkBorder}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnRefresh" Content="Refresh" Padding="12,6" Margin="0,0,8,0" Click="BtnRefresh_Click"/>
                <Button x:Name="btnStop" Content="Stop" Padding="12,6" Click="BtnStop_Click"/>
                <TextBlock x:Name="tbUrl" Text="" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" TextTrimming="CharacterEllipsis" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>
        <wv2:WebView2 x:Name="webView2" Grid.Row="1"/>
    </Grid>
</Window>